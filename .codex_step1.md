ENGJOY — Step 1: Add emoji thumbnail + background color to story cards in library

Goal: Make each story card in Thu Vien tab visually distinct with a thumbnail emoji and colored background.

What to do:

1. src/types/index.ts — Add to Story type:
   - thumbnail: string (the main emoji for this story, e.g. 'turtle emoji' for Tortoise and Hare)
   - color: string (hex background color for the card, e.g. '#F0FFF0' for green tint)

2. src/db/stories.ts — Add thumbnail + color to all 6 existing stories:
   - The Tortoise and the Hare: thumbnail='turtle emoji', color='#F0FFF0' (green tint)
   - The Boy Who Cried Wolf: thumbnail='sheep emoji', color='#FFF8E0' (warm yellow)
   - The Lion and the Mouse: thumbnail='lion emoji', color='#FFF0F0' (warm pink)
   - The Fox and the Grapes: thumbnail='fox emoji', color='#F5E6FF' (purple tint)
   - The Ant and the Grasshopper: thumbnail='ant emoji', color='#E8F5E9' (mint)
   - The Paper Airplane: thumbnail='paper airplane emoji', color='#E3F2FD' (sky blue)
   USE THE ACTUAL EMOJI CHARACTERS, not text descriptions.

3. src/read.ts — Update StoryCard type to include:
   - thumbnail: string
   - color: string
   Update buildStoryCards() to pass these from the Story object.

4. app/(tabs)/library.tsx — Redesign StoryCardItem:
   - Left side: large thumbnail emoji (fontSize: 48) on a colored circle/square background
   - The background color uses story.color
   - Right side: title + level badge
   - Keep the action row at bottom with "Doc" button
   - Make cards more visually appealing with the colored thumbnail area

5. app/library/[id].tsx — Add a colored banner at top of story reader using story.color + story.thumbnail

IMPORTANT:
- Do NOT change any other tabs or screens
- Do NOT remove any existing stories
- Do NOT run npm install
- All changes must be backward compatible with existing code
- After done, run: npx tsc --noEmit (must pass)
