ENGJOY — Step 2: Add "Nghe" (Listen) button to story cards in Thu Vien tab

Goal: Let users tap a "Nghe" button on each story card to hear the full story read aloud via TTS directly from the library screen, without entering the reader.

What to do:

1. app/(tabs)/library.tsx:
   - Add TTS import: let Speech = null; try { Speech = require('expo-speech'); } catch {}
   - In StoryCardItem, add a second button next to "Doc" button in actionRow:
     Button: "Nghe" with a headphone emoji
     On press: call Speech.speak() with all story panels' English text joined by ". "
   - Add both buttons side by side: "Doc" and "Nghe"
   - The StoryCard type in src/read.ts already has all fields (id, routeId, title, thumbnail, color, emojiPreview, levelLabel)
   - But StoryCard does NOT have story text. So you need to look up the story text from libraryStories when the "Nghe" button is pressed.
   - Option A (preferred): Add a `panelsText: string` field to StoryCard in src/read.ts that pre-joins all panel English text. Then the TTS button just reads story.panelsText.
   - Option B: Import libraryStories in library.tsx and find the story by matching id

   Use Option A — modify buildStoryCards() in src/read.ts to include `panelsText` in StoryCard.

   Import expo-speech with try/catch pattern:
   ```
   let Speech: any = null;
   try {
     Speech = require("expo-speech");
   } catch {}
   ```

2. src/read.ts:
   - Add `panelsText: string` to StoryCard type
   - In buildStoryCards(), set panelsText by mapping panels to en text and joining with ". "

3. src/read.test.ts:
   - Update the test to expect `panelsText` in the StoryCard object

IMPORTANT:
- Do NOT use router.push for the Nghe button
- The Nghe button calls Speech.speak() directly
- If TTS starts playing, the button should NOT navigate anywhere
- Do NOT change other tabs or screens
- Do NOT remove or change existing Doc button
- Do NOT run npm install
- After done, run: npx tsc --noEmit (must pass)
